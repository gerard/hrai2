* Date shortcut for today (Usage of 'now')
* Improvements to list filtering
    - Support to list => by id
    - Support to list => by range of ids
    - Support to list => by dates, range of dates
    - Support to list => by category
* List output recognizes terminal width
* Drop marked column in entries database and rename date_effective
* Automatically backup through hrai.py
* Fix argv[0] on helper scripts
    * BETTER FIX: Move subcommands to functions
* Keep hrai mark order on hrai list -m
* Mark a expense by cost/date/category (with prompt)
* hrai backup should check if file exists, and have a --force flag
